import { MutationCache, QueryCache } from 'react-query';

export const apiTokenKey = 'token';

export const newKey = 'new';

export const queryCache = new QueryCache();
export const mutationCache = new MutationCache();
